import LiveSandbox from "./components/LiveSandbox.jsx";
import client_myserver from "./assets/client-myserver.png";
import client_webhook from "./assets/client-webhook.png";

# Lesson 11 – Http Datenübertragen

## Daten mit Requests senden

In der Lesson 10 ging es vorallem darum, wie ein Request mit HTML ausgelöst werden kann.<br/>
Die Antwort war in der Regel eine neue HTML Seite. zBsp. die Suchergebnisseite von Google.<br/>
Es wurden bei diesen Requests auch Daten übermittelt, zBsp. der Suchbegriff.

```text
https://www.google.ch/search?q=HTML+Formulare
```
Aus Formularen können aber auch komplexere Daten an einen Server gesendet werden.<br/>
ZBsp _Name und Adresse_ an den fiktiven Server _meinServer.bbw.ch_ <br/>

```text
Paul Muster
Pionierstrasse 28
8404 Winterthur
```

```text
#Get Request mit URL-Parametern
https://meinServer.bbw.ch/personen?name=Paul+Muster&street=Pionierstrasse+28&plz=8404&city=Winterthur
```
Beim Post Request werden die Daten im **Body** des Requests gesendet.<br/>
Beispiel:
```text
#Post Request mit Body
POST https://meinServer.bbw.ch/personen
Content-Type: application/x-www-form-urlencoded
name=Paul+Muster&street=Pionierstrasse+28&plz=8404&city=Winterthur
```

Oder heute häufiger beim Post Request im JSON Format:<br/>
Beispiel:
```text
#Post Request mit JSON
POST https://meinServer.bbw.ch/personen
Content-Type: application/json
{
  "name": "Paul Muster",
  "street": "Pionierstrasse 28",
  "plz": 8404,
  "city": Winterthur"
}
```
So sieht das Zusammenspiel Client-Server bei einem HTTP Request aus:

<img src={client_myserver} alt="Client-MyServer-http" style={{ height: "400px" }} />

---

## Übertragene Daten sichtbar machen und analysieren

Nun werden Sie **Daten an einen Server schicken** und die **übertragen Daten sichtbar machen** und analysieren.

Den Request lösen Sie hier in der Lesson aus.<br/>
Dann können Sie die übertragenen Daten im Netzwerk Tab der Developer Tools des Browsers analysieren.<br/>
Und zusätzlich verwenden wir einen **Web-hook Dienst**, um die übertragenen Daten sichtbar zu machen.

---

## Interaktive Übung Analyse mit Developer Tools (Get)

Es ist ein einfaches Formular vorbereite.<br/>
ACHTUNG den Webserver myserver.bbw.ch gibt es nicht wirklich.<br/>
Trotzdem können wir die Daten des Requests untersuchen.<br/>
Dazu verwenden zunächst die Developer-Tools des Browsers (Netzwerk-Tab).

**Aufgabe:**

- Lesen Sie zunächst die Seite 2 aus dem Dokument: 25.3_HTTPRequestAnalysieren.pdf.
- Geben Sie in das Formular einen Vornamen und Nachnamen ein und lösen Sie den Request aus.
- Untersuchen Sie den Request im Netzwerk-Tab der Developertools.
    - Header und Payload

weiter:

- Frage: Wo stehen im Formular stehen die Bezeichnungen der Felder im Payload?

<LiveSandbox
    storageKey="11_HttpDevTools01"
    code={
        `function Formular() {
     const [lastName, setLastName] = React.useState("");
     const [preName, setPreName] = React.useState("");
     const [result, setResult] = React.useState(null);

     async function handleSubmit(e) {
       e.preventDefault();
       const url =
         "https://myserver.bbw.ch/get" +
         "?name=" + encodeURIComponent(lastName) +
         "&vorname=" + encodeURIComponent(preName);
       const response = await fetch(url);
       const data = await response.json();
       setResult(data);
     }

     return (
       <div>
         <form onSubmit={handleSubmit}>
            <div><input
              placeholder="Name"
              value={lastName}
              onChange={e => setLastName(e.target.value)}
            /></div>
            <div><input
              placeholder="Vorname"
              value={preName}
              onChange={e => setPreName(e.target.value)}
            /></div>
            <button>Senden (GET)</button>
         </form>

         {result && (
           <>
             <h4>Server-Antwort</h4>
             <pre>{JSON.stringify(result.args, null, 2)}</pre>
           </>
         )}
       </div>
     );
  }`
}
/>

---

## Interaktive Übung Analyse mit Developer Tools (Post)

Nun verwendet das Beispiel _post_ statt _get_ .

**Aufgabe:**

- Geben Sie in das Formular einen Vornamen und Nachnamen ein und lösen Sie den Request aus.
- Untersuchen Sie den Request im Netzwerk-Tab der Developertools.
  - Header und Payload

weiter:

- Frage: Was ist der Unterschied im Payload zwischen Get und Post?
- Frage: Was ist der Unterschied im Code zwischen Get und Post?

<LiveSandbox
    storageKey="11_HttpDevTools02"
    code={
        `function Formular() {
     const [lastName, setLastName] = React.useState("");
     const [preName, setPreName] = React.useState("");
     const [result, setResult] = React.useState(null);

     async function handleSubmit(e) {
       e.preventDefault();
       const url = "https://myserver.bbw.ch/post";
       const response = await fetch(url, {
         method: "POST",
         headers: {
           "Content-Type": "application/json"
         },
         body: JSON.stringify({
           name: lastName,
           vorname: preName
         })
       });
       const data = await response.json();
       setResult(data);
     }

    return (
      <div>
        <form onSubmit={handleSubmit}>
           <div><input
             placeholder="Name"
             value={lastName}
             onChange={e => setLastName(e.target.value)}
           /></div>
           <div><input
             placeholder="Vorname"
             value={preName}
             onChange={e => setPreName(e.target.value)}
           /></div>
           <button>Senden (POST)</button>
        </form>

        {result && (
          <>
            <h4>Server-Antwort</h4>
            <pre>{JSON.stringify(result.args, null, 2)}</pre>
          </>
        )}
      </div>
    );
  }`
}
/>

---

## Interaktive Übung Analyse mit Webhook (GET)

Nun schicken Sie den Request an einen Webhook Dienst.

Was ist ein Web-hook Dienst?<br/>
Ein Web-hook Dienst ist ein Online-Dienst, der es ermöglicht, **HTTP-Anfragen (Requests) zu empfangen und deren Inhalt anzuzeigen**.<br/>
Er wird für Testzwecke verwendet, um zu überprüfen, welche Daten von einem Client an einen Server gesendet werden.<br/>
Der Vorteil ist, dass der **Client-Server-Kommunikation simuliert** werden kann, ohne einen eigenen Server einrichten zu müssen.

Den Inhalt des Request zeigt der Webhook auf seiner Webseite an.

<img src={client_webhook} alt="Client-Webhook-http" style={{ height: "400px" }} />

**Aufgabe:**

- Lesen Sie zunächst die Seite 3 bis 4 aus dem Dokument: 25.3_HTTPRequestAnalysieren.pdf.

weiter:

- Öffnen Sie in einem neuen Fenster des Browser die Webseite: https://webhook.site/
- Ändern Sie unten im vorbereiteten Formular die URL auf Ihre Webhook URL.
- Geben Sie in das Formular einen Vornamen und Nachnamen ein und lösen Sie den Request aus.
- Schauen Sie im Browser-Fenster der Webhook.site die Reaktion an.
    - Untersuchen Header, Query strings und Form values

<LiveSandbox
    storageKey="11_HttpDevTools03"
    code={
        `function Formular() {
     const [lastName, setLastName] = React.useState("");
     const [preName, setPreName] = React.useState("");
     const [result, setResult] = React.useState(null);

     async function handleSubmit(e) {
       e.preventDefault();
       const url =
         "https://webhook.site/c6576e09-ed9b-4403-9623-f81d8078d590" +
         "?name=" + encodeURIComponent(lastName) +
         "&vorname=" + encodeURIComponent(preName);
       const response = await fetch(url);
       const data = await response.json();
       setResult(data);
     }

     return (
       <div>
         <form onSubmit={handleSubmit}>
            <div><input
              placeholder="Name"
              value={lastName}
              onChange={e => setLastName(e.target.value)}
            /></div>
            <div><input
              placeholder="Vorname"
              value={preName}
              onChange={e => setPreName(e.target.value)}
            /></div>
            <button>Senden (GET)</button>
         </form>

         {result && (
           <>
             <h4>Server-Antwort</h4>
             <pre>{JSON.stringify(result.args, null, 2)}</pre>
           </>
         )}
       </div>
     );
  }`
}
/>

---

## Interaktive Übung Analyse mit Webhook (POST)

Nun schicken Sie eine Post Request an einen Webhook Dienst.

**Aufgabe:**

- Ändern Sie unten im vorbereiteten Formular die URL auf Ihre Webhook URL.
- Geben Sie in das Formular einen Vornamen und Nachnamen ein und lösen Sie den Request aus.
- Schauen Sie im Browser-Fenster der Webhook.site die Reaktion an.
  - Untersuchen Header, Query strings und Form values

weiter:

- Ist der Request mit header und body aufgebaut?
- Welche Daten werden dort für die Vorbereitung übertragen?
- War das bei GET Request auch so?

<LiveSandbox
    storageKey="11_HttpDevTools04"
    code={
        `function Formular() {
     const [lastName, setLastName] = React.useState("");
     const [preName, setPreName] = React.useState("");
     const [result, setResult] = React.useState(null);

     async function handleSubmit(e) {
       e.preventDefault();
       const url = "https://webhook.site/c6576e09-ed9b-4403-9623-f81d8078d590";
       const body =
         "name=" + encodeURIComponent(lastName) +
         "&vorname=" + encodeURIComponent(preName);
       const response = await fetch(url, {
         method: "POST",
         headers: {
           "Content-Type": "application/x-www-form-urlencoded"
         },
         body: body
       });

       const data = await response.json();
       setResult(data);
     }

     return (
       <div>
         <form onSubmit={handleSubmit}>
            <div><input
              placeholder="Name"
              value={lastName}
              onChange={e => setLastName(e.target.value)}
            /></div>
            <div><input
              placeholder="Vorname"
              value={preName}
              onChange={e => setPreName(e.target.value)}
            /></div>
            <button>Senden (POST)</button>
         </form>

         {result && (
           <>
             <h4>Server-Antwort</h4>
             <pre>{JSON.stringify(result.args, null, 2)}</pre>
           </>
         )}
       </div>
     );
    }`
}
/>

---

## Übung Antwort via Webhook simulieren

**Aufgabe:**

- Lesen Sie zunächst die Seite 5 aus dem Dokument: 25.3_HTTPRequestAnalysieren.pdf.

weiter:

**Aufgabe:**

- Erstellen Sie auf der Webseite des Webhook eine Antwort.
- Probieren Sie aus, was die Antwort macht, wie Sie hier auf der Session Seite erscheint.
- Welcher Code hier auf der Session Seite wirkt? Empfangen wo? Darstellen wo?

Idee für Antwort auf der Webhook Seite:

```text
Content type: application/json
Content:
{
  "args": {
    "name": "Muster",
    "vorname": "Paul"
  }
}
```
---

## Aufgabe: Lernziele überprüfen

Sie finden auf der Lernplattform die Datei 02.0_M293_KannListeModul293_2025.pdf<br/>
Schauen Sie sich die Lernziele unter _03 http Protokoll und Anfragemethoden_ an.<br/>
Haken Sie die Lernziele ab, die Sie erreicht haben.<br/>

Fragen Sie die Lehrperson bei Unklarheiten.

---



## Aufgabe: Unterschied Webhook.site zu Postman.com

**Aufgabe:**

- Postman ist sehr mächtig. Oft wird es verwendet um ein Application-Interface (API) zu testen.
- Finden Sie heraus, was der Hauptunterschied zwischen Webhook.site und Postman.com ist.<br/>
Was der Unterschied zwischen dem ist, was wir gemacht haben und dem, was Postman beim Testen einer API macht.
 - Recherchieren
 - Austauschen mit Lernpartner

## Übung Unterschied Webhook.site zu Postman.com

**Aufgabe:**

- Postman ist sehr mächtig. Oft wird es verwendet um ein Application-Interface (API) zu testen.
- Finden Sie heraus, was der Hauptunterschied zwischen Webhook.site und Postman.com ist.<br/>
  Was der Unterschied zwischen dem ist, was wir gemacht haben und dem, was Postman beim Testen einer API macht.
   - Recherchieren
   - Austauschen mit Lernpartner

